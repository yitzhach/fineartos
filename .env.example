# Worker secrets: configure in Cloudflare, never VITE_* or frontend source.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_OR_ANON_KEY
# Private R2 IMAGES binding belongs in the opt-in wrangler.cloud.jsonc.
# Local-only mode deliberately does not expose a sign-in UI.
