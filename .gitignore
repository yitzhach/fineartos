node_modules
dist
.env
.env.local
*.local
acceptance-output
tsconfig.tsbuildinfo
# Photographs waiting to be prepared: `npm run wallpapers` writes the web
# versions into public/. The originals are the artist's, not the repo's.
wallpaper-source
